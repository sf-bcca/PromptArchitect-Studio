import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.promptarchitect.app',
  appName: 'PromptArchitectStudio',
  webDir: 'dist'
};

export default config;
